<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Update Pet</h1>
    <form action="/pets/<%= @pet.id %>" method="post">
      <label id="pet[name]">Name:</label>

      <br></br>
      <label id="pet[name]"></label>
      <p><input id="pet_name" type="text" name="pet[name]" value="<%= @pet.name %>"></p>

      <br></br>

      <label>Choose an existing pet:</label>

      <br></br>
      <label id="owner_name">Name:</label>
      <% Owner.all.each do |owner| %> 
        <input type="radio" name="pet[owner_id]" id="<%= owner.id %>" value="<%= owner.id %>" <%= 'checked' if @pet.owner.name == owner.name %>>
          <%= owner.name %>
        </input>
      <% end %>

      <br></br>

      <label>and/or, create a new owner:</label>
      <br></br>
      <label>name:</label>
        <input type="text" name="owner[name]" id="owner_name"></input>
      <br></br>
        <input type="submit" value="Update Pet">
    </form>
  </body>
</html>
